// import React from 'react'
import ReactDOM from 'react-dom/client';
import App from './App.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
	// this is for testing, it forces re-renders and stuff
	// <React.StrictMode>
	<App />
	// </React.StrictMode>,
);
